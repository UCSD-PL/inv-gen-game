# 1 "/tmp/tmpORc9jF.cpp"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 1 "<command-line>" 2
# 1 "/tmp/tmpORc9jF.cpp"

int NONDET;
extern void __VERIFIER_error() __attribute__ ((__noreturn__));
# 15 "/tmp/tmpORc9jF.cpp"
int main() {
  int i, sn=0;
  int SIZE;

  assume(SIZE >= 0);
  for(i=1; i<=SIZE; i++) {
    sn = sn + (2);
  }
  assert(sn==SIZE*(2) || sn == 0);
}
