# 1 "/tmp/tmp7d6prC.cpp"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 1 "<command-line>" 2
# 1 "/tmp/tmp7d6prC.cpp"

int NONDET;
extern void __VERIFIER_error() __attribute__ ((__noreturn__));

extern void assert(int cond);
# 16 "/tmp/tmp7d6prC.cpp"
extern int NONDET;
int main() {
  int i, n, sn=0;
  assume(n < 1000 && n >= -1000);
  for(i=1; i<=n; i++) {
    sn = sn + (2);
  }
  assert(sn==n*(2) || sn == 0);
}
