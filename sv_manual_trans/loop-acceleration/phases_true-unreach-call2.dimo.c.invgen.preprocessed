# 1 "/tmp/tmpR3rIbq.cpp"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 1 "<command-line>" 2
# 1 "/tmp/tmpR3rIbq.cpp"

int NONDET;
extern void __VERIFIER_error() __attribute__ ((__noreturn__));
extern unsigned int __VERIFIER_nondet_uint(void);
# 15 "/tmp/tmpR3rIbq.cpp"
int main(void) {
  unsigned int x = 1;
  unsigned int y;

  assume(y > 0);

  while (x < y) {
    if (x < y / x) {
      x *= x;
    } else {
      x++;
    }
  }

  assert(x == y);
}
