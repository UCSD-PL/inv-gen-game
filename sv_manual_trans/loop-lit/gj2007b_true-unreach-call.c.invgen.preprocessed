# 1 "/tmp/tmppH5XRe.cpp"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 1 "<command-line>" 2
# 1 "/tmp/tmppH5XRe.cpp"

int NONDET;




int main() {
    int x = 0;
    int m = 0;
    int n;
    while(x < n) {
 if(NONDET) {
     m = x;
 }
 x = x + 1;
    }
    assert((m >= 0 || n <= 0));
    assert((m < n || n <= 0));
    return 0;
}
