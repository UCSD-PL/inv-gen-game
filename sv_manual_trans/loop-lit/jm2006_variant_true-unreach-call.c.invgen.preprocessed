# 1 "/tmp/tmpc7Ns_J.cpp"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 1 "<command-line>" 2
# 1 "/tmp/tmpc7Ns_J.cpp"

int NONDET;





int main() {
    int i, j;
    i;
    j;

    assume(i >= 0 && i <= 2147483647);
    assume(j >= 0);
    int x = i;
    int y = j;
    int z = 0;
    while(x != 0) {
        x --;
        y -= 2;
        z ++;
    }
    if (i == j) {
        assert(y == -z);
    }
    return 0;
}
