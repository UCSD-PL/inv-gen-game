# 1 "/tmp/tmpFvoA5U.cpp"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 1 "<command-line>" 2
# 1 "/tmp/tmpFvoA5U.cpp"

int NONDET;



int main() {
    int i, n, a, b;
    i = 0; a = 0; b = 0; n;
    assume(n >= 0 && n <= 2147483647);
    while (i < n) {
        if (NONDET) {
            a = a + 1;
            b = b + 2;
        } else {
            a = a + 2;
            b = b + 1;
        }
        i = i + 1;
    }
    assert(a + b == 3*n);
    return 0;
}
