# 1 "/tmp/tmpq17fnH.cpp"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 1 "<command-line>" 2
# 1 "/tmp/tmpq17fnH.cpp"

int NONDET;
extern void assume(int);





void main()
{
 int n;
 assume(n>0);
 int a = 1;
 int su = 1;
 int t = 1;
 while(su <= n) {
   t = t + 2;
   su = su + t;
   a = a + 1;
 }
 assert((a*a == su));
}
