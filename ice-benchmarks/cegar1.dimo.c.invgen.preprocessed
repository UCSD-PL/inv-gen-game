# 1 "/tmp/tmpgdkEXD.cpp"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 1 "<command-line>" 2
# 1 "/tmp/tmpgdkEXD.cpp"

int NONDET;
# 14 "/tmp/tmpgdkEXD.cpp"
void cegar1(int x, int y) {

 int input;
 if (!(0 <= x))
  return;
 if (!(x <= 2))
  return;
 if (!(0 <= y))
  return;
 if (!(y <= 2))
  return;

 input;
  while ( input) {

  x = x + 2;
  y = y + 2;

  input;
 }
 assert(!((x == 4) && (y == 0)));

}
int main()
{
  int x,y;
  cegar1(x, y);
}
