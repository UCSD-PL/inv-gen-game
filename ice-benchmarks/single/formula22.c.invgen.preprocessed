# 1 "/tmp/tmpJJT08z.cpp"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 1 "<command-line>" 2
# 1 "/tmp/tmpJJT08z.cpp"

int NONDET;
# 13 "/tmp/tmpJJT08z.cpp"
main()
{
  int x1,x2,x3;
  int x1p,x2p,x3p, input;

  x1 = x2 = x3 = 0;
  input;
  while(input)
  {
    x1p;
    x2p;
    x3p;

    if (x1p <= x2p && (x2p >= 0 || x2p - x3p <= 2))
    {
 x1 = x1p;
 x2 = x2p;
 x3 = x3p;
    }
    input;
  }
  assert(x1 <= x2 && (x2 >= 0 || x2 - x3 <= 2));
}
