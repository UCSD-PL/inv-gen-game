# 1 "/tmp/tmpOiLryr.cpp"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 1 "<command-line>" 2
# 1 "/tmp/tmpOiLryr.cpp"

int NONDET;
# 13 "/tmp/tmpOiLryr.cpp"
int main() {

 int a[5];
 int len = 0;
 int i, N;
 int input;

        if (N <= 0)
         return;

 while ( input ) {

  if (len == N)
   len = 0;

  if (len < 0 || len >= N+1)
   assert( 0 == 1);

  len++;

  input;
 }

    return 1;

}
