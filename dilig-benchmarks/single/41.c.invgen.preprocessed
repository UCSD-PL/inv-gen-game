# 1 "/tmp/tmpv3ye8r.cpp"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 1 "<command-line>" 2
# 1 "/tmp/tmpv3ye8r.cpp"

int NONDET;





int main(int n, int flag) {
   assume(n>=0);
   int k = 1;
   int unknown;
   if(flag) {
 k = unknown;
 assume(k>=0);
   }
   int i = 0, j = 0;
   while(i <= n) {
     i++;
     j+=i;
   }
   int z = k + i + j;
   assert(z > 2*n);
}
