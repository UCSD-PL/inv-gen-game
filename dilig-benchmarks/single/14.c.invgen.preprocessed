# 1 "/tmp/tmpdnpED_.cpp"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 1 "<command-line>" 2
# 1 "/tmp/tmpdnpED_.cpp"

int NONDET;





int main() {
  int a = 0;
  int j;
  int m;
  int __BLAST_NONDET;
  if(m<=0)
    return 0;
  for(j = 1; j <= m ; j++){
    if(NONDET)
       a++;
    else
       a--;
  }
  assert(a>=-m);
  assert(a<=m);
}
