# 1 "/tmp/tmp4qeHJS.cpp"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 1 "<command-line>" 2
# 1 "/tmp/tmp4qeHJS.cpp"

int NONDET;






void main(int n, int m)
{
  assume(n>=0);
  assume(m>=0);
  assume(m<n);
  int x=0;
  int y=m;
  while(x<n) {
    x++;
    if(x>m) y++;
  }
  assert(y==n);
}
