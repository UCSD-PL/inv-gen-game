# 1 "/tmp/tmpJ36znK.cpp"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 1 "<command-line>" 2
# 1 "/tmp/tmpJ36znK.cpp"

int NONDET;
extern int NONDET;
extern void assume(int);

void main()
{
  int j;
  int i = 0;
  int k;
  int l;

  assume(i*j<=k);
  while (i < 1000)

  {
    i = i + 1;
    k = k + j;
  }
  assert(i*j<=k);
}
